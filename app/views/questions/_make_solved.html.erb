            <% if user_signed_in? && current_user.id == question.user_id %>
                <%= question.solved ? '解決済み' : '回答受付中' %>
                <% if question.solved %>
                    <%= link_to '回答受付中に戻す', make_solved_question_path(question.id, post: {solved: false}), method: :put, remote: true %>
                <% else %>
                    <%= link_to '解決済みにする', make_solved_question_path(question.id, post: {solved: true}), method: :put, remote: true %>
                <% end %>
            <% end %><br />