<h1>Top#index</h1>
<p>Find me in app/views/top/index.html.erb</p>
<p>質問と講義一覧を載せるトップページ</p>

<h3>質問を検索</h3>
<%= form_tag({controller:"top",action:"index"}, method: :get) do %>
    <%= text_field_tag :search %>
    <%= submit_tag '検索する'  %>
<% end %>

<h3>質問一覧</h3>
<div class="questions-container">
    <% @questions.order("updated_at desc").each do |t| %>
        <div class="question">
            <%= image_tag t.user.image_url,class:"rounded-circle", size: "40x40" if t.user.image? %> <%# アイコン表示 %>
            <%= t.user.name %><br />
            <%= image_tag t.image_url, size: "250x200" if t.image? %><br />
            <% if user_signed_in? %>
                <% if current_user.already_liked?(t) %>
                    <%= link_to question_like_path(id: t.id, question_id: t.id), method: :delete do %>
                        <p>いいねを取り消す</p><%= t.likes.count %>
                    <% end %>
                <% else %>
                    <%= link_to question_likes_path(id: t.id, question_id: t.id), method: :post do %>
                        <p>いいね</p><%= t.likes.count %>
                    <% end %>
                <% end %>
            <% else %>
                <p>いいねの数 = </p><%= t.likes.count %>
            <% end %>
            <%= t.updated_at.to_s(:datetime_jp) %><br />
            <%= link_to "詳細へ", question_path(t.id) %>
            <%= link_to "編集する", edit_question_path(t.id) %>
            <%= link_to "削除する", question_path(t.id), method: :delete %>
        </div>
    <% end %>
</div>

<h3>現在受講中の講義</h3>
<div class="current-lectures-container">
    <% @current_lectures.each do |l| %>
        <div class="current-lecture">
            <%= l.lecture.name %>
            <%= link_to "詳細へ", lecture_path(l.lecture.id) %>
        </div>
    <% end %>
</div>

<h3>過去に受講した講義</h3>
<div class="past-lectures-container">
    <% @past_lectures.each do |l| %>
        <div class="past-lecture">
            <%= l.lecture.name %>
            <%= link_to "詳細へ", lecture_path(l.lecture.id) %>
        </div>
    <% end %>
</div>